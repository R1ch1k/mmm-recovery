# Byte-compiled / optimised
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
build/
dist/
*.egg-info/
.eggs/

# Environments
.venv/
venv/
env/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Editors / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# NOTE: results/ is deliberately NOT ignored. grid.csv and dashboard.html are
# committed artefacts — the study is only reproducible if its outputs are in
# the history alongside the code that made them.

# Local AI-tooling config. Machine-specific absolute paths; never part of the study.
.claude/
.codex/
.cursor/
.agents/
